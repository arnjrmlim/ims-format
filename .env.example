# IMS Format currently has no required environment variables.
#
# Runtime settings are stored by the Windows macro helper under the user's
# APPDATA directory. Do not commit real .env files or local settings.
